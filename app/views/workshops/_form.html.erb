<div class='container mt-5'>
  <%= render 'alert' %>

  <%= form_for @workshop do |f| %>
    <div class='form-group'>
      <%= f.label(:title, 'Título') %>
      <%= f.text_field(:title, value: @workshop.title, class: 'form-control') %>
    </div>

    <div class='form-group'>
      <%= f.label(:categories, 'Categorias') %>
      <%= f.text_field(:categories, value: @workshop.categories, class: 'form-control') %>
    </div>

    <div class='form-group'>
      <%= f.label(:resume, 'Resumo') %>
      <%= f.text_field(:resume,  value: @workshop.resume, class: 'form-control') %>
    </div>

    <div class='form-group'>
      <%= f.label(:document, 'Documento') %>
      <%= f.file_field(:document, value: @workshop.document, class: 'form-control') %>
    </div>

    <%= f.submit('Salvar', class: 'btn btn-success')%>
    <%= link_to 'Voltar', workshops_path, class: 'btn btn-info'%>
  <% end %>
</div>
